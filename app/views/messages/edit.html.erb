<% provide(:title, "メッセージを編集") %>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <h1>メッセージを編集</h1>
    <%= render 'form' %>
    <%= link_to "キャンセル", message_path(@message), class: "btn btn-link" %>
  </div>
</div>
